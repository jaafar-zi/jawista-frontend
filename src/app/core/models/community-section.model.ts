export interface ContentSection {
  titleKey: string;
  descriptionKey: string;
  imageUrl: string;
  imageAltKey: string;
  isHovered?: boolean;
}

export interface MousePosition {
  x: number;
  y: number;
}